import { ActionOptions, OpenAIConnectionGlobalActionContext } from "gadget-server";

/**
 * @param { OpenAIConnectionGlobalActionContext } context
 */
export async function run({ params, logger, api, connections }) {
  // Your logic goes here
};

/** @type { ActionOptions } */
export const options = {};
