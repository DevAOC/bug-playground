import { ActionOptions, TestingGlobalActionContext } from "gadget-server";

/**
 * @param { TestingGlobalActionContext } context
 */
export async function run({ params, logger, api, connections }) {
  // Your logic goes here
};

/** @type { ActionOptions } */
export const options = {};
